{
  "name": "plataforma-aut",
  "version": "0.1.0",
  "private": true,
  "description": "Marketplace de anúncios de automóveis — monorepo (Next.js + FastAPI)",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "postcss": "^8.5.26",
    "sharp": "^0.35.3"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "cd apps/api && uv run uvicorn src.main:app --reload --port 8000",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/web",
    "test:api": "cd apps/api && uv run python -m pytest",
    "lint:api": "cd apps/api && uv run ruff check . && uv run ruff format --check .",
    "typecheck:api": "cd apps/api && uv run python -m mypy src",
    "check": "npm run lint && npm run typecheck && npm run lint:api && npm run typecheck:api && npm run test:api"
  }
}
